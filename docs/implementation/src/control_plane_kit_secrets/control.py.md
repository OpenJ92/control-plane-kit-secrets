Source: [src/control_plane_kit_secrets/control.py](../../../../src/control_plane_kit_secrets/control.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This sole Core-importing source module owns the Secrets public receiving protocol: frozen typed configuration, exact V2 liveness-only/control-socket declaration, closed JSON codec/encoder and required environment-selected public file. It uses actual Core and purpose-specific SDK verifier types; it owns no graph artifact/product, store, private credential or signing-family semantics. Encoding revalidates typed shape and decodes the emitted bytes before returning them, including forged-value rejection.

The explicit absolute path has a4096 UTF-8 byte ceiling and noNUL/default/fallback. The opened descriptor must be regular and uses no-follow/nonblocking/close-on-exec flags; bounded reads consume at most65,537 bytes, with a65,536-byte document ceiling. The descriptor closes in finally even when stream construction or decoding fails. Public0444 is lawful; private mode/readability checks remain in their existing separate loader. Parent-directory integrity and authorized producer delivery are assumptions, not claims made by these checks.

Closed field/key-family admission and actual SDK canonical public-key validation reject malformed/private-shaped inputs. Ordinary failures leave as a fixed SecretsControlConfigurationError outside the active exception handler; BaseException propagates. Separate static/health verifier preparation captures the supplied clock and returns a real dispatcher whose only callback reports local process liveness; it performs no provider auth/store/audit work. API owns SDK route installation on the complete host, and Servers189 owns public artifact construction/delivery. [Receiver tests](../../../../tests/test_control_configuration.py) and [startup/authority tests](../../../../tests/test_control_receiver.py) protect this boundary.
