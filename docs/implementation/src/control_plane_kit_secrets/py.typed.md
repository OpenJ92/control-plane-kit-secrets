Source: [src/control_plane_kit_secrets/py.typed](../../../../src/control_plane_kit_secrets/py.typed).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This empty package-data marker advertises inline typing under the packaging convention. It adds no runtime validation or public exports. [pyproject.toml](../../../../pyproject.toml) includes it in the distribution; keep that packaging link when moving the source tree. Its presence is not evidence that a type checker ran or every public input is dynamically validated.
