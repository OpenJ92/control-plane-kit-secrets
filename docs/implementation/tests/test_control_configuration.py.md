Source: [tests/test_control_configuration.py](../../../tests/test_control_configuration.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Five tests require the named public receiver explicitly before testing typed value/codec round trips, closed malformed inputs, roles/key-family/declaration rejection and encoder revalidation, public0444 regular opened files, required absolute bounded path and size, observed no-follow/nonblocking/close-on-exec flags, fixed redaction and BaseException propagation. Exact65,536-byte acceptance/plus-one rejection and invalid ASCII/UTF-8 path rejection before OS access strengthen the boundary checks. Initial red proved only interface absence; these downstream assertions require implementation execution. IO observations forward to real file opens for the lawful case; error injection is confined to the OS boundary. No FIFO that could block under a regression is opened. Temporary files are synthetic and isolated; these are receiver admission laws, not proof of production mount provenance or hostile-parent protection.
