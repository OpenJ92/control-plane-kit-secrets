Source: [tests/test_control_receiver.py](../../../tests/test_control_receiver.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Five tests govern required API control/initializer arguments, invalid control and actual reserved-route collision before initialization, failed public startup before private loaders, one real custody initializer after inspection of complete SDK/provider routes, and signed static/liveness reads with isolated purpose/target/runtime/instance authority. The app factory observation supplies an ordinary real FastAPI host; the SDK installer is not replaced with a thrown exception. A collision may leave newly defined ordinary routes on that unreturned test host, but must publish no canonical SDK routes and invoke no initializer. Private/store/audit observers reject any unexpected work during SDK reads; real custody is created only in temporary fixture context. Failure must return no app. Initial red stopped at absent interface/signature assertions; actual downstream evidence must come from the reviewed implementation gate, independently of predecessor-fixture translations.

At initializer entry, the actual health route must match Core's canonical name/path/method and fully match concrete GET /__control/health/liveness with health_kind=liveness. This inspects real route matching without serving the app before initialization. Static/ordinary route presence, absence of prior custody, exactly-once initialization and failed-initializer no-return assertions remain. The first implementation gate exposed a literal-route assumption in this fixture; later assertions in that method were unreached, so the corrected fixture requires a new reviewed owning gate.
