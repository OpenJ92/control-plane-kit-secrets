Source: [tests/test_sdk_compatibility.py](../../../tests/test_sdk_compatibility.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Three tests establish the intended dependency compatibility checkpoint: exact declared runtime/test requirements and Python metadata; installed Core/SDK commit-archive provenance plus selected framework/crypto versions; and representative actual SDK composition on the existing provider app. Archive metadata identifies selected source, not a new independent cryptographic audit or complete transitive dependency lock. Read [pyproject.toml](../../../pyproject.toml) with the actual selected SDK metadata on adoption.

The composition fixture uses real temporary SQLite custody and the existing [api.py](../../../src/control_plane_kit_secrets/api.py), with empty provider credentials, independent generated in-memory static/health signing keys and actual typed SDK verifiers. Its valid test-only V2 declaration advertises liveness on socket control, with no mutable variables. It checks an exact Core canonical static response, a Core-decoded signed liveness result, missing and wrong-purpose denial, one liveness observation, and no audit rows. It preserves existing route objects and regenerates OpenAPI after installation before comparing the full schema, so a pre-install cache cannot stand in for that proof. Legacy health/docs and provider authentication denial remain observable.

The temporary master-key file is0600; generated authority is local to the existing test container, with no credential material or derived hashes in evidence. These tests do not install a production receiver, prove public artifact delivery or establish startup effect ordering. Existing signing/custody/auth/bootstrap/restart tests remain the wider compatibility laws; #26 owns required production control configuration and health admission. Run only the owning [test.sh](../../../test.sh) after its approved preflight, and report executable results separately from this description.
