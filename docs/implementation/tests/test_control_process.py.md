Source: [tests/test_control_process.py](../../../tests/test_control_process.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

One actual-process target law covers missing setting, absent file and malformed public configuration while supplying valid generated private master input and an empty credential document. Each case uses the existing Uvicorn loopback launch/stop helpers and temporary SQLite path. It requires bounded nonzero startup exit, fixed redacted control error and no DB/parent creation. At the pre-receiver target-red checkpoint, the old provider may ignore the new input and create temporary custody; after10 seconds the existing terminate/kill discipline stops it before the missing-law assertion fails. Captured pipes are not continuously byte-capped, and the port probe is not an atomic reservation. No user credential, host execution or external provider is involved; only the owning Docker gate may run this reviewed fixture scope.
